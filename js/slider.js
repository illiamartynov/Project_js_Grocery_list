$(document).on('ready', function() {
    $(".your_class").slick({
    dots: true,
    infinite: true,
    centerMode: true,
    slidesToShow: 1,
    slidesToScroll: 1,
    centerMode: true,
    });
});